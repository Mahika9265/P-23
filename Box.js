class box{}
    var options={
        isStatic=false,
        restitution:0.3,
        friction:0.5,
        density:1.2
    }
    var side1;
    var side2;
    var side3;
    side1=createSprite(250,20,100,20);
    side2=createSprite(width/2,20,20,200);
    side3=createSprite(450,20,100,20);